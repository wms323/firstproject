# 环境配置文档

## 硬件信息
**操作系统**: Windows 11
**CUDA版本**: 13.2（驱动支持的最高版本）
**GPU信息**：NVIDIA GeForce RTX 5060 Laptop GPU

## 软件配置
**Anaconda版本**: 25.11.1
**Python版本**: 3.11.15
**虚拟环境名**：pytorch
**环境路径**：`E:\anaconda3\envs\pytorch`

## 安装步骤
1. 安装Anaconda
地址：https://www.anaconda.com/download
2. 创建虚拟环境
打开anaconda prompt，输入以下命令：
conda create -n pytorch python=3.11
conda activate pytorch
3. 安装 PyTorch（CUDA 版）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
（该命令安装的是 CUDA 12.8 版本的 PyTorch，兼容 RTX 5060 显卡）
4. 安装其他依赖
pip install numpy pillow opencv-python tensorboard matplotlib
5. 验证环境
python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA available:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0))"
（输出 `CUDA available: True` 即表示环境搭建成功）

## 验证结果
PyTorch: 2.12.0.dev20260408+cu128
CUDA available: True
CUDA version: 12.8
GPU: NVIDIA GeForce RTX 5060 Laptop GPU